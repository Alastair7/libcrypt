[project]
name = "libcrypt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "click==8.3.1",
    "textual==7.3.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
